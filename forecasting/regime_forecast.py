import pandas as pd


def run_regime_forecasts(
    returns,
    hmm_models: dict,
    ms_garch_models: dict,
    horizon: int,
):
    """Run regime‑aware forecasts for HMM and MS‑GARCH (stub)."""
    results = []
    # Implement regime‑conditioned forecasting logic.
    return pd.DataFrame(results)
