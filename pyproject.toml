# pyproject.toml (dependencies section)
[project]
name = "ng-vol-garch"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "pandas",
  "numpy",
  "scikit-learn",
  "arch",
  "pyyaml",
  "matplotlib",
]
